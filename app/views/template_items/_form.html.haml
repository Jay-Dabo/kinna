= render partial: 'shared/search_account', locals: {model: @template, account: 'template_item_account', description: 'template_item_description'}
= simple_form_for([@template, @template_item], wrapper: :horizontal_form, html: {class: 'form-horizontal'}) do |f|
  = f.error_notification
  = f.input :account
  = f.input :description

  = f.input :enable_debit
  = f.input :enable_credit

  .form-button-group
    .actions
      = f.submit class: 'btn btn-primary'
