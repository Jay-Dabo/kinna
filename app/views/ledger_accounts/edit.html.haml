.table-responsive{:ng_controller=>'ModalCtrl'}
  %table{:class => 'table table-striped'}
    %thead
      %tr
        %th= t(:number)
        %th= t(:text)
        %th= t(:sum)
        %th
    %tbody{}
      - @ledger_transactions.each do |ledger_transaction|
        %tr
          %td= ledger_transaction.parent
          %td= ledger_transaction.account.number
          %td= ledger_transaction.sum

  = paginate @ledger_transactions, :theme => 'twitter-bootstrap-3'
  = render partial: 'shared/modal_delete', locals: {idx: 'deleteContent'}